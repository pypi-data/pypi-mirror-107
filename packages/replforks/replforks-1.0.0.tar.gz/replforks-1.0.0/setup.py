from setuptools import setup

setup(
    name='replforks',
    version='1.0.0',
    description='A way to detect forks in python.',
    long_description='I will add.',
    packages=["replforks"]
  )

