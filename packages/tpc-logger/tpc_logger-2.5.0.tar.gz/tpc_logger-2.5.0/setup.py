from setuptools import setup, find_packages

setup(
   name='tpc_logger',
   version='2.5.0',
   description='A logger',
   author='Romain Osso',
   author_email='romain.osso@theprogrammaticcompany.com',
   install_requires=['colorama'],
   packages=['tpc_logger']
)