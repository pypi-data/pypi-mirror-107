from orator.migrations import Migration


class Create{{table.nameCapitalize}}Table(Migration):

    def up(self):
        """
        Run the migrations.
        """
        with self.schema.create('{{table.nameLower}}') as table:
            table.increments('id')
            table.timestamps()

    def down(self):
        """
        Revert the migrations.
        """
        self.schema.drop('{{table.nameLower}}')
