from setuptools import setup

setup(name = 'yayuchen_package',
      version = '0.1',
      description = 'Gaussian distributions',
      packages = ['yayuchen_package'],
      zip_safe = False)