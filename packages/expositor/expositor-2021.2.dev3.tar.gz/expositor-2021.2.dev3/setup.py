#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['expositor']

package_data = \
{'': ['*']}

package_dir = \
{'': 'src'}

install_requires = \
['quo >=2021.1.dev4']

extras_require = \
{'all': ['quo >=2021.1.dev4'], 'test': ['quo >=2021.1.dev4']}

setup(name='expositor',
      version='2021.2.dev3',
      description='Spinher is a Python module which shows the user progress whenever a Command-Line application is launched. The `whirl()` function does all the magic.',
      author='Gerrishon Sirere',
      author_email='secretum.inc@pm.me',
      url='https://github.com/gerrishons/expositor',
      packages=packages,
      package_data=package_data,
      package_dir=package_dir,
      install_requires=install_requires,
      extras_require=extras_require,
      python_requires='>=2.7',
     )
