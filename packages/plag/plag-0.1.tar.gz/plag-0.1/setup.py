from setuptools import setup

setup(name='plag',
      version='0.1',
      description='Search for clones of code in the texts of computer programs',
      packages=['plag'],
      author_email='d.e.cherkasov@mail.ru',
      zip_safe=False)